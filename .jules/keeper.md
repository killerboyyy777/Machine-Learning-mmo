# Keeper's Journal

## Learnings & Constraints
- Dependency setup: Python environment requires `pip install -r requirements.txt` (specifically `websockets`) before running test suites if packages are not present in the sandbox environment.
- Linting: Legacy files are exempt from repo-wide lint failures, lint only touched files/lines.
