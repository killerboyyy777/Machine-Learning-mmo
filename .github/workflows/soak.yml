name: Nightly soak

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      agents:
        description: "Concurrent agents"
        default: "50"
      duration:
        description: "Soak duration in seconds"
        default: "3600"

jobs:
  soak:
    name: Conductor soak (50 agents, 1hr)
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install websockets==17.1
      - run: echo '{}' > scores.json
      - run: TEXTMMO_GM_SEED=700 nohup python server.py --config ml/conductor/soak_server_config.json > server.soak.log 2>&1 &
      - run: sleep 5
      - run: python ml/conductor/soak.py --agents ${{ inputs.agents || 50 }} --duration ${{ inputs.duration || 3600 }} --min-agents 40 --server-log server.soak.log --slot linear --slot linear --slot gather --slot dungeon --slot market --slot maker --slot commissioner
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: soak-metrics
          path: |
            soak_run/metrics.jsonl
            soak_run/soak_status.jsonl
            soak_run/soak_report.json
            soak_run/soak.log
            server.soak.log
