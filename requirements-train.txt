# Training-only dependencies (NOT installed by default - see requirements.txt).
#
# Install with:
#     pip install -r requirements-train.txt
#
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu
numpy==2.5.3
